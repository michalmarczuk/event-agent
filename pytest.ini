[pytest]
markers =
    regression: selected local Component and Component Integration tests
    smoke: fast availability checks for critical external integrations
    live: tests that call real external systems
